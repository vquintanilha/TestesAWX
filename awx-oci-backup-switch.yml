---
- name: Run manage_backup.sh
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - name: Execute manage_backup.sh
      ansible.builtin.shell: ./manage-backup.sh
      args:
        chdir: "{{ playbook_dir }}/scripts"
